services:
  pollyspeople:
    image: candrews74/pollyspeople:latest
    container_name: pollyspeople
    restart: unless-stopped
    ports:
      - '4180:80'
    volumes:
      # Mount dogs/ so photos can be added/updated without rebuilding the image
      - /mnt/Data/dogs:/usr/share/nginx/html/dogs:z
